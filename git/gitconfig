[user]
    name = $NAME
    email = $EMAIL

[alias]
    co = checkout
    br = branch
    ci = commit
    st = status

[core]
    editor = code --wait
    excludesfile = ~/.gitignore_global

[push]
    default = simple
